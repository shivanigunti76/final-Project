Instance
Amazon Linux

ebs
	create volu,me-->gp3,20,AZ-us-east-1a(Should be same as ec2)
	Actions->Attach Volume-> instance give then device name as dev/xvdbf

lsblk
sudo fdisk /dev/xvdbf
m n p e e e w
sudo lsblk -fs /dev/xvdbf1
sudo mkfs.xfs /dev/xvdbf1

sudo mkdir /mnt/shivani
sudo mount /dev/xvdbf1 /mnt/Shivani
sudo df -h
sudo blkid /dev/xvdbf1
nano /etc/fstab
UUID=ec0f0354-8ff4-44a2-9f71-f32f781e73a2 /mnt/Shivani xfs defaults,nofail 0 0
Ctrl+o enter  x
sudo mount -a
cd /mnt/Shivani
sudo touch f1
reboot
cd /mnt/Shivani
ls
f1 should appear

then snapshot
volume
actions create snapshot
then snapshot -> copy snap shot choose dest west 2
then move to that region see the snapshot
then create volume from actions create volume

then create ec2
volumes actions attach volume put ec2
ec2 connect
and commands
lsblk
sudo mkdir /mnt/shivani
sudo mount /dev/xvdbf1 /mnt/Shivani
cd /mnt/shivani
ls
you should see f1